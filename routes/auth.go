package routes

import (
	//"github.com/SparkElf/blog-server/middleware"
	"github.com/gin-gonic/gin"
)

func initAuth(server *gin.Engine) {
	//auth := server.Group("api/v1")
	//auth.Use(middleware.JwtToken())

}
